global.jQuery = require('jquery');
(function($) {
  $(document).ready(function() {
    const animation = require('./components/animation.js');
    //    const cursor = require('./components/cursor.js');
    const icons = require('./components/icons');
    const lazyload = require('./components/lazyload');
    const navigation = require('./components/navigation');
    const sliders = require('./components/sliders');
    const videoPlayer = require('./components/video_player.js');
    const maps = require('./components/maps.js');
    const services = require('./components/services.js');
  });
})(jQuery);
